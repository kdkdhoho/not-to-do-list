# 잉걸 오늘 탭 (코어 루프 UI) 구현 계획 (Plan 2/4)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Plan 1의 도메인 코어 위에 "오늘" 탭 전체(스트릭 히어로·습관 리스트·소급 카드·하루 마무리 시트·무너짐 기록 시트)를 잉걸 디자인 시스템으로 구현해 코어 루프를 실제로 사용 가능하게 만든다.

**Architecture:** 잉걸 공용 컴포넌트는 `Shared/Core/DesignSystem/Components/`에 View-only로, 화면은 `Feature/Today/`에 MVVM(Route/Router/NavigationView/View/ViewModel)으로. TodayViewModel은 `CheckInServicing`·`HabitRepository`·`TrackingRepository` 프로토콜만 소비하고, 테스트는 인메모리 SwiftData로 실동작 검증한다. 템플릿 데모(Home/Detail/Explore + Item)는 제거한다.

**Tech Stack:** SwiftUI, @Observable, Swift Testing, Tuist 4

## Global Constraints

- Swift 6 strict concurrency, min iOS 26 (CLAUDE.md)
- 사용자 노출 문자열은 전부 `AppStrings` + `Localizable.xcstrings` (ko 소스, en 별도 집필) — 하드코딩 금지
- 색·폰트·간격·라운딩은 `AppColor`·`AppTypography`·`Theme` 토큰만 — 리터럴 금지 (view-design-system 규칙)
- 잉걸(Brand.primary)은 **하루 마무리 CTA·스트릭 불꽃·오늘 링에만**. frost는 프리즈 표기 전용. 무너짐에 빨강 금지 (design.md)
- CTA 라운딩 = `Theme.CornerRadius.lg`(16). 필(pill)은 상태 칩 전용 (design.md 개정)
- 그림자·그라데이션 금지. 유일한 발광은 불꽃 글로우(`shadow(color: Brand.primary.opacity(0.35), radius: 12)`)
- 아이콘은 `AppIcon` enum 경유 (`Image(appIcon:)`), `Image(systemName:)` 직접 호출 금지
- 화면 좌우 패딩 `Theme.Spacing.screenH`(20), 눌림 피드백 scale 0.97
- UI 용어: 행동 = **"하루 마무리"**, 소급 버튼 = [참았어요][못 참았어요][기억나지 않아요]
- 테스트: `tuist test Shared` (SharedTests·FeatureTests 모두 Shared 스킴 테스트 액션에 붙는다 — Task 3에서 FeatureTests 추가 후에도 동일 명령). Project.swift 수정 후엔 `tuist generate --no-open`
- 커밋 트레일러: `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`

---

### Task 1: AppIcon 확장 + 잉걸 기초 컴포넌트

**Files:**
- Modify: `Sources/Shared/Core/DesignSystem/AppIcon.swift` (case 추가만)
- Create: `Sources/Shared/Core/DesignSystem/Components/EmberCTAButton.swift`
- Create: `Sources/Shared/Core/DesignSystem/Components/SecondaryButton.swift`
- Create: `Sources/Shared/Core/DesignSystem/Components/FreezeChip.swift`

**Interfaces:**
- Produces: `AppIcon` 신규 case: `.flame, .snowflake, .smoke, .forkKnife, .takeoutBag, .wineglass, .cupAndSaucer, .popcorn, .iphone, .playRectangle, .gamecontroller, .archive, .banknote, .medal, .bookOpen`
- Produces: `EmberCTAButton(title: String, action: @escaping () -> Void)` — 높이 56, `Brand.primary` 배경, `Text.inverse` 라벨, `CornerRadius.lg`
- Produces: `SecondaryButton(title: String, isOnSheet: Bool = false, action: @escaping () -> Void)` — 높이 48, `CornerRadius.md`, 배경은 `isOnSheet ? Background.primary : Background.elevated` (시트 위 반전 규칙)
- Produces: `FreezeChip(count: Int)` — pill 칩, frost 표기

- [ ] **Step 1: AppIcon case 추가**

`AppIcon.swift`의 기존 섹션 뒤에 추가 (case 이름 = lucide 이름 camelCase, rawValue = SF Symbol — image-resource 규칙):

```swift
    // MARK: - 잉걸 (Ember)
    case flame = "flame"                              // lucide: flame — 스트릭 불꽃
    case snowflake = "snowflake"                      // lucide: snowflake — 프리즈
    case smoke = "smoke"                              // lucide: cigarette — 담배
    case forkKnife = "fork.knife"                     // lucide: drumstick — 야식
    case takeoutBag = "takeoutbag.and.cup.and.straw"  // lucide: utensils — 배달음식
    case wineglass = "wineglass"                      // lucide: beer — 술
    case cupAndSaucer = "cup.and.saucer"              // lucide: coffee — 카페 음료
    case popcorn = "popcorn"                          // lucide: cookie — 군것질
    case iphone = "iphone"                            // lucide: smartphone — SNS
    case playRectangle = "play.rectangle"             // lucide: youtube — 쇼츠·유튜브
    case gamecontroller = "gamecontroller"            // lucide: gamepad-2 — 게임
    case archive = "archivebox"                       // lucide: archive — 보관
    case banknote = "banknote"                        // lucide: banknote — 아낀 돈
    case medal = "medal"                              // lucide: medal — 배지
    case bookOpen = "book"                            // lucide: book-open — 독서 환산
```

- [ ] **Step 2: 컴포넌트 3종 구현**

`Sources/Shared/Core/DesignSystem/Components/EmberCTAButton.swift`:

```swift
import SwiftUI

/// 하루 마무리 전용 CTA — 잉걸이 허락된 유일한 버튼 (design.md checkin-cta)
public struct EmberCTAButton: View {
    private let title: String
    private let action: () -> Void
    @State private var isPressed = false

    public init(title: String, action: @escaping () -> Void) {
        self.title = title
        self.action = action
    }

    public var body: some View {
        Button(action: action) {
            Text(title)
                .font(AppTypography.bodyStrong)
                .foregroundStyle(AppColor.Text.inverse)
                .frame(maxWidth: .infinity, minHeight: 56)
                .background(
                    isPressed ? AppColor.Brand.primaryPressed : AppColor.Brand.primary,
                    in: RoundedRectangle(cornerRadius: Theme.CornerRadius.lg, style: .continuous)
                )
        }
        .buttonStyle(.plain)
        .scaleEffect(isPressed ? 0.97 : 1)
        .onLongPressGesture(minimumDuration: .infinity) {} onPressingChanged: { pressing in
            withAnimation(.easeOut(duration: 0.08)) { isPressed = pressing }
        }
    }
}
```

`Sources/Shared/Core/DesignSystem/Components/SecondaryButton.swift`:

```swift
import SwiftUI

/// 하루 마무리 이외의 확정 행동 (design.md button-secondary).
/// 시트(surface-raised) 위에서는 배경을 canvas로 반전해 색 단차를 만든다.
public struct SecondaryButton: View {
    private let title: String
    private let isOnSheet: Bool
    private let action: () -> Void

    public init(title: String, isOnSheet: Bool = false, action: @escaping () -> Void) {
        self.title = title
        self.isOnSheet = isOnSheet
        self.action = action
    }

    public var body: some View {
        Button(action: action) {
            Text(title)
                .font(AppTypography.bodyStrong)
                .foregroundStyle(AppColor.Text.primary)
                .frame(maxWidth: .infinity, minHeight: 48)
                .background(
                    isOnSheet ? AppColor.Background.primary : AppColor.Background.elevated,
                    in: RoundedRectangle(cornerRadius: Theme.CornerRadius.md, style: .continuous)
                )
        }
        .buttonStyle(.plain)
    }
}
```

`Sources/Shared/Core/DesignSystem/Components/FreezeChip.swift`:

```swift
import SwiftUI

/// ❄ 프리즈 보유 칩 (design.md freeze-chip). frost는 프리즈 표기 전용 — 인터랙티브 금지.
public struct FreezeChip: View {
    private let count: Int

    public init(count: Int) {
        self.count = count
    }

    public var body: some View {
        HStack(spacing: Theme.Spacing.xs + 2) {
            Image(appIcon: .snowflake)
                .font(.system(size: 14))
                .foregroundStyle(count > 0 ? AppColor.Functional.frost : AppColor.Text.tertiary)
            Text("×\(count)")
                .font(AppTypography.numeralSm)
                .foregroundStyle(count > 0 ? AppColor.Functional.frost : AppColor.Text.tertiary)
        }
        .padding(.vertical, 6)
        .padding(.horizontal, 12)
        .background(
            count > 0 ? AppColor.Functional.frostSoft : AppColor.Background.secondary,
            in: Capsule()
        )
    }
}
```

참고: `Image(appIcon:)` 이니셜라이저가 없다면 `AppIcon.swift`에 이미 있는 패턴을 확인하고 그대로 쓴다 (탐색상 `MainTab.icon: AppIcon`이 존재하므로 이니셜라이저가 이미 있을 가능성이 높다 — 없으면 `Image(systemName: icon.rawValue)`를 쓰는 기존 관례를 따른다).

- [ ] **Step 3: 빌드 검증**

Run: `tuist build Shared`
Expected: 성공 (경고 0)

- [ ] **Step 4: Commit**

```bash
git add Sources/Shared/Core/DesignSystem
git commit -m "feat: 잉걸 아이콘 15종 + EmberCTAButton·SecondaryButton·FreezeChip"
```

---

### Task 2: AppStrings + Localizable.xcstrings (오늘 탭 문자열)

**Files:**
- Modify: `Sources/Shared/Core/DesignSystem/AppStrings.swift`
- Modify: `Sources/Shared/Resources/Localizable.xcstrings`

**Interfaces:**
- Produces: `AppStrings.Tab.today/record/settings`, `AppStrings.Today.*`, `AppStrings.Closing.*`, `AppStrings.Lapse.*`, `AppStrings.Yesterday.*`

- [ ] **Step 1: AppStrings enum 추가**

`AppStrings.swift`에 추가 (기존 `String(localized:bundle:)` 패턴 그대로). 기존 `Tab.home/explore`는 Task 6에서 화면 개편과 함께 제거하므로 여기서는 추가만:

```swift
    public enum Today {
        /// "일째 마무리"
        public static var streakLabel: String { String(localized: "today.streakLabel", bundle: .module) }
        /// "오늘 마무리가 남아 있어요"
        public static var streakLabelOpen: String { String(localized: "today.streakLabelOpen", bundle: .module) }
        /// "오늘 하루 마무리하기"
        public static var closeCTA: String { String(localized: "today.closeCTA", bundle: .module) }
        /// "오늘 하루 마무리했어요 · 내일 봐요"
        public static var closedChip: String { String(localized: "today.closedChip", bundle: .module) }
        /// "첫 습관 등록하기"
        public static var addFirstHabit: String { String(localized: "today.addFirstHabit", bundle: .module) }
        /// "무너짐 기록하기" (행 스와이프 액션)
        public static var lapseAction: String { String(localized: "today.lapseAction", bundle: .module) }
        /// "D+%lld"
        public static func dPlus(_ days: Int) -> String {
            String(localized: "today.dPlus \(days)", bundle: .module)
        }
        /// "%@을 아꼈어요!" (포맷된 금액)
        public static func savedMoney(_ amount: String) -> String {
            String(localized: "today.savedMoney \(amount)", bundle: .module)
        }
        /// "책 %lld권 읽을 시간을 아꼈어요"
        public static func savedBooks(_ books: Int) -> String {
            String(localized: "today.savedBooks \(books)", bundle: .module)
        }
        /// "책 %lld페이지 읽을 시간을 아꼈어요"
        public static func savedPages(_ pages: Int) -> String {
            String(localized: "today.savedPages \(pages)", bundle: .module)
        }
    }

    public enum Closing {
        /// "오늘 하루 마무리할게요"
        public static var title: String { String(localized: "closing.title", bundle: .module) }
        /// "무너진 습관만 표시해요 — 나머지는 참은 거예요"
        public static var subtitle: String { String(localized: "closing.subtitle", bundle: .module) }
        /// "이대로 마무리하기"
        public static var confirm: String { String(localized: "closing.confirm", bundle: .module) }
        /// "낮에 기록했어요 · 잠김"
        public static var lockedCaption: String { String(localized: "closing.lockedCaption", bundle: .module) }
    }

    public enum Lapse {
        /// "무너진 순간을 기록할게요"
        public static var title: String { String(localized: "lapse.title", bundle: .module) }
        /// "메모를 남길 수 있어요 (선택)"
        public static var memoPlaceholder: String { String(localized: "lapse.memoPlaceholder", bundle: .module) }
        /// "기록해도 하루를 마무리하면 스트릭은 그대로예요"
        public static var keepNote: String { String(localized: "lapse.keepNote", bundle: .module) }
        /// "기록하기"
        public static var confirm: String { String(localized: "lapse.confirm", bundle: .module) }
        /// "취소"
        public static var cancel: String { String(localized: "lapse.cancel", bundle: .module) }
    }

    public enum Yesterday {
        /// "어제는 어땠어요?"
        public static var title: String { String(localized: "yesterday.title", bundle: .module) }
        /// "참았어요"
        public static var resisted: String { String(localized: "yesterday.resisted", bundle: .module) }
        /// "못 참았어요"
        public static var lapsed: String { String(localized: "yesterday.lapsed", bundle: .module) }
        /// "기억나지 않아요"
        public static var unknown: String { String(localized: "yesterday.unknown", bundle: .module) }
    }
```

그리고 `Tab`에 추가:

```swift
        public static var today: String { String(localized: "tab.today", bundle: .module) }
        public static var record: String { String(localized: "tab.record", bundle: .module) }
```

- [ ] **Step 2: xcstrings 키 추가 (ko 소스 + en 별도 집필)**

`Localizable.xcstrings`(JSON)에 아래 키를 기존 형식대로 추가한다. en은 직역이 아니라 PRD §14 영어 원칙(sentence case, 축약형, 동사 중심)의 카피다:

| key | ko | en |
|---|---|---|
| tab.today | 오늘 | Today |
| tab.record | 기록 | History |
| today.streakLabel | 일째 마무리 | days closed |
| today.streakLabelOpen | 오늘 마무리가 남아 있어요 | Today's still open |
| today.closeCTA | 오늘 하루 마무리하기 | Close today |
| today.closedChip | 오늘 하루 마무리했어요 · 내일 봐요 | Today's closed · see you tomorrow |
| today.addFirstHabit | 첫 습관 등록하기 | Add your first habit |
| today.lapseAction | 무너짐 기록하기 | Log a slip |
| today.dPlus %lld | D+%lld | D+%lld |
| today.savedMoney %@ | %@을 아꼈어요! | You've saved %@! |
| today.savedBooks %lld | 책 %lld권 읽을 시간을 아꼈어요 | Time saved for %lld books |
| today.savedPages %lld | 책 %lld페이지 읽을 시간을 아꼈어요 | Time saved for %lld pages |
| closing.title | 오늘 하루 마무리할게요 | Let's close today |
| closing.subtitle | 무너진 습관만 표시해요 — 나머지는 참은 거예요 | Mark only the slips — the rest counts as clean |
| closing.confirm | 이대로 마무리하기 | Close it |
| closing.lockedCaption | 낮에 기록했어요 · 잠김 | Logged earlier · locked |
| lapse.title | 무너진 순간을 기록할게요 | Let's log this slip |
| lapse.memoPlaceholder | 메모를 남길 수 있어요 (선택) | Add a note (optional) |
| lapse.keepNote | 기록해도 하루를 마무리하면 스트릭은 그대로예요 | Logging won't break your streak — just close the day |
| lapse.confirm | 기록하기 | Log it |
| lapse.cancel | 취소 | Cancel |
| yesterday.title | 어제는 어땠어요? | How was yesterday? |
| yesterday.resisted | 참았어요 | Stayed strong |
| yesterday.lapsed | 못 참았어요 | Slipped |
| yesterday.unknown | 기억나지 않아요 | Can't remember |

xcstrings 항목 형식(기존 파일과 동일): `"tab.today": {"localizations": {"ko": {"stringUnit": {"state": "translated", "value": "오늘"}}, "en": {"stringUnit": {"state": "translated", "value": "Today"}}}}`. 포맷 인자 키(`today.dPlus %lld` 등)는 String Catalog의 자동 플레이스홀더 규칙을 따른다.

- [ ] **Step 3: 빌드 검증**

Run: `tuist build Shared`
Expected: 성공

- [ ] **Step 4: Commit**

```bash
git add Sources/Shared/Core/DesignSystem/AppStrings.swift Sources/Shared/Resources/Localizable.xcstrings
git commit -m "feat: 오늘 탭 문자열 — AppStrings + ko/en 카탈로그"
```

---

### Task 3: FeatureTests 타겟 + TodayViewModel (TDD)

**Files:**
- Modify: `Project.swift` (FeatureTests 타겟 추가)
- Create: `Sources/Feature/Today/TodayViewModel.swift`
- Test: `Tests/FeatureTests/TodayViewModelTests.swift`

**Interfaces:**
- Consumes: `CheckInServicing`(refresh/closeToday/closeYesterday/recordImmediateLapse), `HabitRepository`, `TrackingRepository`, `ProgressCalculator`, `ReadingConversion`, `DayBoundary`, `AppCurrency` (전부 Plan 1)
- Produces:

```swift
@Observable
public final class TodayViewModel {
    public struct HabitRow: Identifiable, Equatable, Sendable {
        public let id: UUID
        public let name: String
        public let icon: String        // AppIcon rawValue
        public let colorHex: String
        public let dPlusText: String   // "D+37"
        public let subtitle: String    // "384,000원을 아꼈어요!" / 독서 환산 / ""
        public let lapsedToday: Bool   // 즉시 기록됨 → 마무리 시트에서 잠김
    }

    public private(set) var streakDays: Int
    public private(set) var freezeCount: Int
    public private(set) var isClosedToday: Bool
    public private(set) var showYesterdayCard: Bool
    public private(set) var habitRows: [HabitRow]
    public private(set) var lastCloseResult: CloseResult?

    public init(checkInService: CheckInServicing, habitRepository: HabitRepository,
                trackingRepository: TrackingRepository, currency: AppCurrency,
                calendar: Calendar = .current)

    public func refresh(now: Date = .now) async
    public func closeToday(lapsedHabitIDs: Set<UUID>, now: Date = .now) async
    public func answerYesterday(_ answer: YesterdayAnswer, now: Date = .now) async  // 모든 활성 습관에 일괄 적용
    public func recordLapse(habitID: UUID, memo: String?, now: Date = .now) async
}
```

- 설계 결정: 소급 카드의 답변은 **어제의 모든 활성 습관에 일괄 적용**한다 (하루 1답 — 습관별 소급 UI는 로드맵). 에러(`CheckInError`)는 조용히 무시하지 않고 `refresh`로 상태 재동기화한다.

- [ ] **Step 1: Project.swift에 FeatureTests 타겟 추가**

`targets:` 배열의 SharedTests 타겟 뒤에 추가:

```swift
        .target(
            name: "FeatureTests",
            destinations: destinations,
            product: .unitTests,
            bundleId: "\(bundleId).featureTests",
            deploymentTargets: .iOS(deploymentTarget),
            infoPlist: .default,
            buildableFolders: ["Tests/FeatureTests"],
            dependencies: [
                .target(name: "Feature"),
            ],
            settings: .settings(
                base: [
                    "DEVELOPMENT_TEAM": "QD353RFHM5",
                ]
            )
        ),
```

- [ ] **Step 2: 실패하는 테스트 작성**

`Tests/FeatureTests/TodayViewModelTests.swift`:

```swift
import Foundation
import Testing
import Shared
@testable import Feature

@MainActor
struct TodayViewModelTests {
    let cal = Calendar(identifier: .gregorian)

    func fixture() throws -> (TodayViewModel, HabitRepository, CheckInServicing) {
        let container = try AppModelContainer.make(inMemory: true)
        let habits = SwiftDataHabitRepository(modelContainer: container)
        let tracking = SwiftDataTrackingRepository(modelContainer: container)
        let service = CheckInService(habitRepository: habits, trackingRepository: tracking,
                                     currency: AppCurrency(code: "KRW"), calendar: cal)
        let vm = TodayViewModel(checkInService: service, habitRepository: habits,
                                trackingRepository: tracking,
                                currency: AppCurrency(code: "KRW"), calendar: cal)
        return (vm, habits, service)
    }

    var now: Date { cal.date(from: DateComponents(year: 2026, month: 7, day: 8, hour: 21))! }
    var today: DayStamp { DayBoundary.dayStamp(for: now, calendar: cal) }

    func seedHabit(_ repo: HabitRepository, rate: Habit.Rate = .money(3000),
                   daysAgo: Int = 9) async throws -> Habit {
        let created = today.advanced(by: -daysAgo, calendar: cal)
        let h = Habit(id: UUID(), name: "담배", icon: "smoke", colorHex: "D4B15F",
                      rate: rate, createdDay: created, firstTrackedDay: created)
        try await repo.save(h)
        return h
    }

    @Test func 리프레시가_스트릭과_습관행을_조립한다() async throws {
        let (vm, habits, _) = try fixture()
        _ = try await seedHabit(habits)
        await vm.refresh(now: now)
        #expect(vm.habitRows.count == 1)
        #expect(vm.habitRows[0].dPlusText == "D+10")           // 관대 모델: 등록일 포함 10일
        #expect(vm.habitRows[0].subtitle.contains("30,000"))   // 10일 × 3,000 포맷 포함
        #expect(vm.isClosedToday == false)
        #expect(vm.streakDays == 0)
    }

    @Test func 시간단가는_독서환산_서브타이틀() async throws {
        let (vm, habits, _) = try fixture()
        _ = try await seedHabit(habits, rate: .time(minutes: 60), daysAgo: 19)   // 20일×60분=1200분=책2권
        await vm.refresh(now: now)
        #expect(vm.habitRows[0].subtitle == AppStrings.Today.savedBooks(2))
    }

    @Test func 마무리하면_상태가_닫힘으로_바뀐다() async throws {
        let (vm, habits, _) = try fixture()
        _ = try await seedHabit(habits)
        await vm.refresh(now: now)
        await vm.closeToday(lapsedHabitIDs: [], now: now)
        #expect(vm.isClosedToday == true)
        #expect(vm.streakDays == 1)
        #expect(vm.lastCloseResult != nil)
    }

    @Test func 어제_미마무리면_소급카드가_보인다() async throws {
        let (vm, habits, service) = try fixture()
        let h = try await seedHabit(habits)
        // 그제 마무리 기록을 만들어 어제만 공백으로 둔다
        let dayBefore = cal.date(from: DateComponents(year: 2026, month: 7, day: 6, hour: 21))!
        _ = try await service.closeToday(lapsedHabitIDs: [], now: dayBefore)
        await vm.refresh(now: now)
        #expect(vm.showYesterdayCard == true)
        await vm.answerYesterday(.resisted, now: now)
        #expect(vm.showYesterdayCard == false)
        #expect(vm.streakDays == 2)
        _ = h
    }

    @Test func 즉시기록하면_행이_잠김표시된다() async throws {
        let (vm, habits, _) = try fixture()
        let h = try await seedHabit(habits)
        await vm.refresh(now: now)
        await vm.recordLapse(habitID: h.id, memo: "회식", now: now)
        #expect(vm.habitRows[0].lapsedToday == true)
    }
}
```

- [ ] **Step 3: 실패 확인**

Run: `tuist generate --no-open && tuist test Shared`
Expected: 컴파일 실패 — `cannot find 'TodayViewModel' in scope`

- [ ] **Step 4: 구현**

`Sources/Feature/Today/TodayViewModel.swift`:

```swift
import Foundation
import Observation
import Shared

@Observable
@MainActor
public final class TodayViewModel {
    public struct HabitRow: Identifiable, Equatable, Sendable {
        public let id: UUID
        public let name: String
        public let icon: String
        public let colorHex: String
        public let dPlusText: String
        public let subtitle: String
        public let lapsedToday: Bool
    }

    public private(set) var streakDays = 0
    public private(set) var freezeCount = 0
    public private(set) var isClosedToday = false
    public private(set) var showYesterdayCard = false
    public private(set) var habitRows: [HabitRow] = []
    public private(set) var lastCloseResult: CloseResult?

    private let checkInService: CheckInServicing
    private let habitRepository: HabitRepository
    private let trackingRepository: TrackingRepository
    private let currency: AppCurrency
    private let calendar: Calendar

    public init(checkInService: CheckInServicing, habitRepository: HabitRepository,
                trackingRepository: TrackingRepository, currency: AppCurrency,
                calendar: Calendar = .current) {
        self.checkInService = checkInService
        self.habitRepository = habitRepository
        self.trackingRepository = trackingRepository
        self.currency = currency
        self.calendar = calendar
    }

    public func refresh(now: Date = .now) async {
        do {
            let decision = try await checkInService.refresh(now: now)
            streakDays = decision.state.current
            freezeCount = decision.state.freezes
            let today = DayBoundary.dayStamp(for: now, calendar: calendar)
            isClosedToday = try await trackingRepository.close(on: today) != nil
            showYesterdayCard = decision.yesterdayOpenForBackfill && !(try await activeYesterday(now: now).isEmpty)
            habitRows = try await buildRows(today: today)
        } catch {
            // 로드 실패 시 상태 유지 — 빈 화면 튐 방지
        }
    }

    public func closeToday(lapsedHabitIDs: Set<UUID>, now: Date = .now) async {
        do {
            lastCloseResult = try await checkInService.closeToday(lapsedHabitIDs: lapsedHabitIDs, now: now)
        } catch {
            lastCloseResult = nil
        }
        await refresh(now: now)
    }

    public func answerYesterday(_ answer: YesterdayAnswer, now: Date = .now) async {
        do {
            let habits = try await activeYesterday(now: now)
            let answers = Dictionary(uniqueKeysWithValues: habits.map { ($0.id, answer) })
            _ = try await checkInService.closeYesterday(answers: answers, now: now)
        } catch {}
        await refresh(now: now)
    }

    public func recordLapse(habitID: UUID, memo: String?, now: Date = .now) async {
        do {
            let trimmed = memo?.trimmingCharacters(in: .whitespacesAndNewlines)
            try await checkInService.recordImmediateLapse(
                habitID: habitID, memo: (trimmed?.isEmpty ?? true) ? nil : trimmed, now: now)
        } catch {}
        await refresh(now: now)
    }

    // MARK: - Private

    private func activeYesterday(now: Date) async throws -> [Habit] {
        let yesterday = DayBoundary.dayStamp(for: now, calendar: calendar).advanced(by: -1, calendar: calendar)
        return try await habitRepository.activeHabits(on: yesterday)
    }

    private func buildRows(today: DayStamp) async throws -> [HabitRow] {
        let habits = try await habitRepository.activeHabits(on: today)
        let todayMarks = try await trackingRepository.marks(in: today...today)
        let lapsedTodayIDs = Set(todayMarks.compactMap { mark -> UUID? in
            if case .lapsed = mark.kind { return mark.habitID }
            return nil
        })
        var rows: [HabitRow] = []
        for habit in habits {
            let marks = try await trackingRepository.marks(habitID: habit.id)
            let progress = ProgressCalculator.progress(for: habit, marks: marks,
                                                       today: today, calendar: calendar)
            rows.append(HabitRow(
                id: habit.id, name: habit.name, icon: habit.icon, colorHex: habit.colorHex,
                dPlusText: AppStrings.Today.dPlus(progress.currentRun),
                subtitle: subtitle(for: progress.saved),
                lapsedToday: lapsedTodayIDs.contains(habit.id)))
        }
        return rows
    }

    private func subtitle(for saved: SavedAmount) -> String {
        switch saved {
        case .money(let amount):
            let formatted = amount.formatted(.currency(code: currency.code))
            return AppStrings.Today.savedMoney(formatted)
        case .time(let minutes):
            let books = ReadingConversion.books(fromMinutes: minutes)
            if books >= 1 { return AppStrings.Today.savedBooks(books) }
            return AppStrings.Today.savedPages(ReadingConversion.pages(fromMinutes: minutes))
        case .none:
            return ""
        }
    }
}
```

주의: 테스트의 `subtitle.contains("30,000")`은 KRW `FormatStyle` 출력(₩30,000)을 전제한다. 로케일에 따라 구분자가 다르면 테스트에서 `vm.habitRows[0].subtitle == AppStrings.Today.savedMoney(Decimal(30000).formatted(.currency(code: "KRW")))`로 비교를 바꾼다 — 기대값을 하드코딩하지 말고 같은 포맷터로 만든 문자열과 비교한다.

- [ ] **Step 5: 통과 확인**

Run: `tuist test Shared`
Expected: PASS (FeatureTests 5 + 기존 SharedTests 전부)

- [ ] **Step 6: Commit**

```bash
git add Project.swift Sources/Feature/Today/TodayViewModel.swift Tests/FeatureTests/TodayViewModelTests.swift
git commit -m "feat: TodayViewModel — 오늘 탭 상태 조립 (TDD)"
```

---

### Task 4: 오늘 탭 화면 컴포넌트 (StreakHearth·HabitRow·YesterdayCard)

**Files:**
- Create: `Sources/Feature/Today/Views/StreakHearthView.swift`
- Create: `Sources/Feature/Today/Views/HabitRowCard.swift`
- Create: `Sources/Feature/Today/Views/YesterdayCardView.swift`

**Interfaces:**
- Consumes: Task 1 컴포넌트, Task 2 문자열, `TodayViewModel.HabitRow`
- Produces: `StreakHearthView(streakDays: Int, isClosedToday: Bool, freezeCount: Int)`, `HabitRowCard(row: TodayViewModel.HabitRow)`, `YesterdayCardView(onAnswer: @escaping (YesterdayAnswer) -> Void)`

- [ ] **Step 1: 구현**

`Sources/Feature/Today/Views/StreakHearthView.swift`:

```swift
import SwiftUI
import Shared

/// 홈 상단 히어로 — 카드가 아니다, 화면 그 자체가 화로다 (design.md streak-hearth)
struct StreakHearthView: View {
    let streakDays: Int
    let isClosedToday: Bool
    let freezeCount: Int

    var body: some View {
        VStack(alignment: .leading, spacing: Theme.Spacing.sm) {
            HStack(alignment: .top) {
                HStack(alignment: .bottom, spacing: Theme.Spacing.sm + 4) {
                    Image(appIcon: .flame)
                        .font(.system(size: 44))
                        .foregroundStyle(AppColor.Brand.primary)
                        .shadow(color: AppColor.Brand.primary.opacity(0.35), radius: 12)
                    Text("\(streakDays)")
                        .font(AppTypography.numeralHero)
                        .tracking(AppTypography.Tracking.numeralHero)
                        .foregroundStyle(AppColor.Text.primary)
                }
                Spacer()
                FreezeChip(count: freezeCount)
            }
            Text(isClosedToday ? AppStrings.Today.streakLabel : AppStrings.Today.streakLabelOpen)
                .font(AppTypography.caption)
                .foregroundStyle(AppColor.Text.secondary)
                .padding(.leading, 56)
        }
        .accessibilityElement(children: .combine)
    }
}
```

`Sources/Feature/Today/Views/HabitRowCard.swift`:

```swift
import SwiftUI
import Shared

/// 습관 리스트 행 — 습관의 색은 44px 원 안에만 산다 (design.md habit-row)
struct HabitRowCard: View {
    let row: TodayViewModel.HabitRow

    var body: some View {
        HStack(spacing: Theme.Spacing.sm + 4) {
            ZStack {
                Circle()
                    .fill(Color(hex: row.colorHex).opacity(0.12))
                    .frame(width: 44, height: 44)
                Image(systemName: row.icon)
                    .font(.system(size: 20))
                    .foregroundStyle(Color(hex: row.colorHex))
            }
            VStack(alignment: .leading, spacing: 2) {
                Text(row.name)
                    .font(AppTypography.bodyStrong)
                    .foregroundStyle(AppColor.Text.primary)
                if !row.subtitle.isEmpty {
                    Text(row.subtitle)
                        .font(AppTypography.caption)
                        .foregroundStyle(AppColor.Text.secondary)
                }
            }
            Spacer()
            Text(row.dPlusText)
                .font(AppTypography.numeralSm)
                .foregroundStyle(AppColor.Text.primary)
        }
        .padding(Theme.Spacing.md)
        .background(AppColor.Background.secondary,
                    in: RoundedRectangle(cornerRadius: Theme.CornerRadius.lg, style: .continuous))
    }
}
```

`Sources/Feature/Today/Views/YesterdayCardView.swift`:

```swift
import SwiftUI
import Shared

/// 소급 확인 카드 — 어느 답도 강조하지 않는다 (design.md yesterday-card)
struct YesterdayCardView: View {
    let onAnswer: (YesterdayAnswer) -> Void

    var body: some View {
        VStack(alignment: .leading, spacing: Theme.Spacing.md) {
            Text(AppStrings.Yesterday.title)
                .font(AppTypography.heading)
                .foregroundStyle(AppColor.Text.primary)
            ViewThatFits(in: .horizontal) {
                HStack(spacing: Theme.Spacing.sm) { answerButtons }
                VStack(spacing: Theme.Spacing.sm) { answerButtons }   // SE·영어 라벨 폴백
            }
        }
        .padding(Theme.Spacing.lg)
        .background(AppColor.Background.secondary,
                    in: RoundedRectangle(cornerRadius: Theme.CornerRadius.lg, style: .continuous))
    }

    @ViewBuilder private var answerButtons: some View {
        answerButton(AppStrings.Yesterday.resisted, .resisted)
        answerButton(AppStrings.Yesterday.lapsed, .lapsed)
        answerButton(AppStrings.Yesterday.unknown, .unknown)
    }

    private func answerButton(_ title: String, _ answer: YesterdayAnswer) -> some View {
        Button { onAnswer(answer) } label: {
            Text(title)
                .font(AppTypography.caption)
                .fontWeight(.semibold)
                .foregroundStyle(AppColor.Text.primary)
                .frame(maxWidth: .infinity, minHeight: 40)
                .background(AppColor.Background.elevated,
                            in: RoundedRectangle(cornerRadius: Theme.CornerRadius.md, style: .continuous))
        }
        .buttonStyle(.plain)
    }
}
```

참고: `Image(systemName: row.icon)`은 `HabitRowCard`에서만 예외적으로 허용 — 습관 아이콘은 사용자 데이터(동적 rawValue)라 `AppIcon` enum을 거칠 수 없다. 이 결정을 코드 주석 없이 그대로 두면 규칙 위반으로 오해되므로 위 코드처럼 유지한다.

- [ ] **Step 2: 빌드 검증**

Run: `tuist build Feature`
Expected: 성공

- [ ] **Step 3: Commit**

```bash
git add Sources/Feature/Today/Views
git commit -m "feat: 오늘 탭 뷰 컴포넌트 — StreakHearth·HabitRowCard·YesterdayCard"
```

---

### Task 5: TodayView + 시트 2종 + 라우팅

**Files:**
- Create: `Sources/Feature/Today/TodayRoute.swift`
- Create: `Sources/Feature/Today/TodayRouter.swift`
- Create: `Sources/Feature/Today/TodayNavigationView.swift`
- Create: `Sources/Feature/Today/TodayView.swift`
- Create: `Sources/Feature/Today/Views/ClosingSheetView.swift`
- Create: `Sources/Feature/Today/Views/LapseSheetView.swift`

**Interfaces:**
- Consumes: Task 3 `TodayViewModel`, Task 4 컴포넌트
- Produces: `TodayRouter`(`path`, `sheet: TodaySheet?`), `TodaySheet: Identifiable` (`.closing`, `.lapse(habitID: UUID)`), `TodayNavigationView(router:viewModel:)`

- [ ] **Step 1: 라우팅 구현**

`Sources/Feature/Today/TodayRoute.swift`:

```swift
import Foundation

public enum TodayRoute: Hashable {
    case habitDetail(habitID: UUID)   // Plan 3에서 목적지 구현 — 지금은 push 미사용
}

public enum TodaySheet: Identifiable, Hashable {
    case closing
    case lapse(habitID: UUID)

    public var id: String {
        switch self {
        case .closing: "closing"
        case .lapse(let id): "lapse-\(id.uuidString)"
        }
    }
}
```

`Sources/Feature/Today/TodayRouter.swift`:

```swift
import Observation
import SwiftUI

@Observable
public final class TodayRouter {
    public var path = NavigationPath()
    public var sheet: TodaySheet?

    public init() {}

    public func navigate(to route: TodayRoute) { path.append(route) }
    public func pop() { path.removeLast() }
    public func popToRoot() { path.removeLast(path.count) }
    public func presentSheet(_ sheet: TodaySheet) { self.sheet = sheet }
    public func dismissSheet() { sheet = nil }
}
```

`Sources/Feature/Today/TodayNavigationView.swift`:

```swift
import SwiftUI
import Shared

public struct TodayNavigationView: View {
    @Bindable private var router: TodayRouter
    private let viewModel: TodayViewModel

    public init(router: TodayRouter, viewModel: TodayViewModel) {
        self.router = router
        self.viewModel = viewModel
    }

    public var body: some View {
        NavigationStack(path: $router.path) {
            TodayView(viewModel: viewModel, router: router)
        }
        .sheet(item: $router.sheet) { sheet in
            switch sheet {
            case .closing:
                ClosingSheetView(viewModel: viewModel, router: router)
                    .presentationDetents([.medium, .large])
                    .presentationBackground(AppColor.Background.elevated)
            case .lapse(let habitID):
                LapseSheetView(viewModel: viewModel, router: router, habitID: habitID)
                    .presentationDetents([.medium])
                    .presentationBackground(AppColor.Background.elevated)
            }
        }
    }
}
```

- [ ] **Step 2: TodayView 구현**

`Sources/Feature/Today/TodayView.swift`:

```swift
import SwiftUI
import Shared

public struct TodayView: View {
    let viewModel: TodayViewModel
    let router: TodayRouter

    public var body: some View {
        ZStack(alignment: .bottom) {
            AppColor.Background.primary.ignoresSafeArea()

            ScrollView {
                VStack(alignment: .leading, spacing: Theme.Spacing.md) {
                    StreakHearthView(streakDays: viewModel.streakDays,
                                     isClosedToday: viewModel.isClosedToday,
                                     freezeCount: viewModel.freezeCount)
                        .padding(.top, Theme.Spacing.sm)
                        .padding(.bottom, Theme.Spacing.xl)   // 불꽃에게 숨 쉴 공간 (합계 48)

                    if viewModel.showYesterdayCard {
                        YesterdayCardView { answer in
                            Task { await viewModel.answerYesterday(answer) }
                        }
                    }

                    LazyVStack(spacing: Theme.Spacing.sm) {
                        ForEach(viewModel.habitRows) { row in
                            HabitRowCard(row: row)
                                .contextMenu {
                                    Button(AppStrings.Today.lapseAction) {
                                        router.presentSheet(.lapse(habitID: row.id))
                                    }
                                }
                        }
                    }

                    Spacer(minLength: 96)   // 하단 고정 CTA 공간
                }
                .padding(.horizontal, Theme.Spacing.screenH)
            }

            bottomBar
                .padding(.horizontal, Theme.Spacing.screenH)
                .padding(.bottom, Theme.Spacing.md)
        }
        .task { await viewModel.refresh() }
        .toolbarVisibility(.hidden, for: .navigationBar)
    }

    @ViewBuilder private var bottomBar: some View {
        if viewModel.isClosedToday {
            Text(AppStrings.Today.closedChip)
                .font(AppTypography.body)
                .foregroundStyle(AppColor.Text.secondary)
                .frame(maxWidth: .infinity, minHeight: 56)
                .background(AppColor.Background.secondary, in: Capsule())
        } else if viewModel.habitRows.isEmpty {
            EmberCTAButton(title: AppStrings.Today.addFirstHabit) {
                // 습관 추가 플로우는 Plan 3 — 현재는 비활성 자리만 유지
            }
        } else {
            EmberCTAButton(title: AppStrings.Today.closeCTA) {
                router.presentSheet(.closing)
            }
        }
    }
}
```

- [ ] **Step 3: 시트 2종 구현**

`Sources/Feature/Today/Views/ClosingSheetView.swift`:

```swift
import SwiftUI
import Shared

/// 하루 마무리 플로우 시트 — 기본 전원 "참았어요", 무너진 습관만 탭해서 전환 (design.md closing-sheet)
struct ClosingSheetView: View {
    let viewModel: TodayViewModel
    let router: TodayRouter
    @State private var lapsedIDs: Set<UUID> = []

    var body: some View {
        VStack(alignment: .leading, spacing: Theme.Spacing.md) {
            Text(AppStrings.Closing.title)
                .font(AppTypography.heading)
                .foregroundStyle(AppColor.Text.primary)
            Text(AppStrings.Closing.subtitle)
                .font(AppTypography.caption)
                .foregroundStyle(AppColor.Text.secondary)

            ScrollView {
                VStack(spacing: 0) {
                    ForEach(viewModel.habitRows) { row in
                        closingRow(row)
                        if row.id != viewModel.habitRows.last?.id {
                            Rectangle().fill(AppColor.Border.primary).frame(height: 1)
                        }
                    }
                }
            }

            EmberCTAButton(title: AppStrings.Closing.confirm) {
                let ids = lapsedIDs
                Task {
                    await viewModel.closeToday(lapsedHabitIDs: ids)
                    router.dismissSheet()
                }
            }
        }
        .padding(Theme.Spacing.lg)
        .onAppear {
            lapsedIDs = Set(viewModel.habitRows.filter(\.lapsedToday).map(\.id))
        }
    }

    private func closingRow(_ row: TodayViewModel.HabitRow) -> some View {
        let isLapsed = lapsedIDs.contains(row.id)
        return Button {
            guard !row.lapsedToday else { return }   // 즉시 기록은 잠김
            if isLapsed { lapsedIDs.remove(row.id) } else { lapsedIDs.insert(row.id) }
        } label: {
            HStack(spacing: Theme.Spacing.sm + 4) {
                ZStack {
                    Circle().fill(Color(hex: row.colorHex).opacity(0.12)).frame(width: 40, height: 40)
                    Image(systemName: row.icon)
                        .font(.system(size: 18))
                        .foregroundStyle(Color(hex: row.colorHex))
                }
                VStack(alignment: .leading, spacing: 2) {
                    Text(row.name)
                        .font(AppTypography.bodyStrong)
                        .foregroundStyle(AppColor.Text.primary)
                    if row.lapsedToday {
                        Text(AppStrings.Closing.lockedCaption)
                            .font(AppTypography.label)
                            .foregroundStyle(AppColor.Text.tertiary)
                    }
                }
                Spacer()
                stateGlyph(isLapsed: isLapsed, locked: row.lapsedToday)
            }
            .padding(.vertical, Theme.Spacing.sm + 4)
        }
        .buttonStyle(.plain)
    }

    @ViewBuilder private func stateGlyph(isLapsed: Bool, locked: Bool) -> some View {
        ZStack {
            if isLapsed {
                Circle().strokeBorder(AppColor.Text.primary, lineWidth: 2)
                if locked {
                    Image(appIcon: .lock)
                        .font(.system(size: 11))
                        .foregroundStyle(AppColor.Text.tertiary)
                }
            } else {
                Circle().fill(AppColor.Text.primary)
                Image(appIcon: .check)
                    .font(.system(size: 14, weight: .semibold))
                    .foregroundStyle(AppColor.Background.primary)
            }
        }
        .frame(width: 28, height: 28)
    }
}
```

`Sources/Feature/Today/Views/LapseSheetView.swift`:

```swift
import SwiftUI
import Shared

/// 즉시 무너짐 기록 시트 — 확정 버튼은 잉걸이 아니다 (design.md lapse-sheet)
struct LapseSheetView: View {
    let viewModel: TodayViewModel
    let router: TodayRouter
    let habitID: UUID
    @State private var memo = ""

    private var habitRow: TodayViewModel.HabitRow? {
        viewModel.habitRows.first { $0.id == habitID }
    }

    var body: some View {
        VStack(alignment: .leading, spacing: Theme.Spacing.md) {
            Text(AppStrings.Lapse.title)
                .font(AppTypography.heading)
                .foregroundStyle(AppColor.Text.primary)

            if let row = habitRow {
                HStack(spacing: Theme.Spacing.sm + 4) {
                    ZStack {
                        Circle().fill(Color(hex: row.colorHex).opacity(0.12)).frame(width: 40, height: 40)
                        Image(systemName: row.icon)
                            .font(.system(size: 18))
                            .foregroundStyle(Color(hex: row.colorHex))
                    }
                    Text(row.name)
                        .font(AppTypography.bodyStrong)
                        .foregroundStyle(AppColor.Text.primary)
                }
            }

            TextField(AppStrings.Lapse.memoPlaceholder, text: $memo)
                .font(AppTypography.body)
                .foregroundStyle(AppColor.Text.primary)
                .padding(.horizontal, Theme.Spacing.md)
                .frame(minHeight: 48)
                .background(AppColor.Background.primary,
                            in: RoundedRectangle(cornerRadius: Theme.CornerRadius.md, style: .continuous))

            Text(AppStrings.Lapse.keepNote)
                .font(AppTypography.caption)
                .foregroundStyle(AppColor.Text.secondary)

            SecondaryButton(title: AppStrings.Lapse.confirm, isOnSheet: true) {
                let id = habitID
                let note = memo
                Task {
                    await viewModel.recordLapse(habitID: id, memo: note)
                    router.dismissSheet()
                }
            }

            Button(AppStrings.Lapse.cancel) { router.dismissSheet() }
                .font(AppTypography.bodyStrong)
                .foregroundStyle(AppColor.Text.secondary)
                .frame(maxWidth: .infinity)
        }
        .padding(Theme.Spacing.lg)
    }
}
```

- [ ] **Step 4: 빌드 검증**

Run: `tuist build Feature`
Expected: 성공

- [ ] **Step 5: Commit**

```bash
git add Sources/Feature/Today
git commit -m "feat: TodayView + 하루 마무리·무너짐 기록 시트 + 라우팅"
```

---

### Task 6: 탭 개편 + 템플릿 데모 제거 + DI 통합

**Files:**
- Modify: `Sources/Feature/Tab/MainTab.swift` — case를 `today, record, settings`로 교체
- Modify: `Sources/Feature/Tab/MainTabView.swift` — Today/Record/Settings 배선으로 교체
- Modify: `Sources/Feature/Navigation/AppRouter.swift` — `today = TodayRouter()`, `record`/`settings` 유지 구조로 교체, `DeepLink.itemDetail` 제거 → `case today`
- Create: `Sources/Feature/Record/RecordPlaceholderView.swift`
- Delete: `Sources/Feature/Home/` 전체, `Sources/Feature/Detail/` 전체, `Sources/Feature/Explore/` 전체
- Modify: `Sources/ServiceApp/AppDIContainer.swift` — `makeTodayViewModel()` 추가, `makeHomeViewModel`/`makeDetailViewModel`/`itemRepository` 제거
- Modify: `Sources/ServiceApp/RootView.swift` — MainTabView 새 시그니처로
- Modify: `Sources/Shared/Core/DesignSystem/AppStrings.swift` — `Tab.home/explore`, `Home`, `Explore`, `Detail` enum 제거
- Test: 기존 FeatureTests 전부 + 전체 빌드

**Interfaces:**
- Consumes: Task 3·5의 Today 스택
- Produces: `MainTab.today/.record/.settings`, `AppRouter.today: TodayRouter`, `AppDIContainer.makeTodayViewModel() -> TodayViewModel`

- [ ] **Step 1: MainTab·MainTabView·AppRouter 교체**

`Sources/Feature/Tab/MainTab.swift`:

```swift
import Foundation
import Shared

public enum MainTab: String, CaseIterable {
    case today
    case record
    case settings

    public var title: String {
        switch self {
        case .today: AppStrings.Tab.today
        case .record: AppStrings.Tab.record
        case .settings: AppStrings.Tab.settings
        }
    }

    public var icon: AppIcon {
        switch self {
        case .today: .house
        case .record: .calendar
        case .settings: .settings
        }
    }
}
```

`Sources/Feature/Navigation/AppRouter.swift` — 기존 구조 유지하며 교체:

```swift
import Observation

public enum DeepLink: Hashable {
    case today   // 위젯 탭 → 오늘 탭 (PRD §10)
}

@Observable
public final class AppRouter {
    public var selectedTab: MainTab = .today

    public let today = TodayRouter()
    public let settings = SettingsRouter()

    private var pendingLink: DeepLink?

    public init() {}

    public func handle(_ link: DeepLink) {
        switch link {
        case .today:
            selectedTab = .today
        }
    }

    public func enqueue(_ link: DeepLink) { pendingLink = link }

    public func flushPendingLink() {
        guard let link = pendingLink else { return }
        pendingLink = nil
        handle(link)
    }
}
```

`Sources/Feature/Tab/MainTabView.swift`:

```swift
import SwiftUI
import Shared

public struct MainTabView: View {
    @Bindable private var appRouter: AppRouter
    private let todayViewModel: TodayViewModel

    public init(appRouter: AppRouter, todayViewModel: TodayViewModel) {
        self.appRouter = appRouter
        self.todayViewModel = todayViewModel
    }

    public var body: some View {
        TabView(selection: $appRouter.selectedTab) {
            TodayNavigationView(router: appRouter.today, viewModel: todayViewModel)
                .tabItem { Label(MainTab.today.title, systemImage: MainTab.today.icon.rawValue) }
                .tag(MainTab.today)

            RecordPlaceholderView()
                .tabItem { Label(MainTab.record.title, systemImage: MainTab.record.icon.rawValue) }
                .tag(MainTab.record)

            SettingsNavigationView(router: appRouter.settings)
                .tabItem { Label(MainTab.settings.title, systemImage: MainTab.settings.icon.rawValue) }
                .tag(MainTab.settings)
        }
        .tint(AppColor.Text.primary)   // 탭에 잉걸 금지 — 탭 이동은 핵심 행동이 아니다
        .task { appRouter.flushPendingLink() }
    }
}
```

참고: `SettingsNavigationView`의 기존 init 시그니처를 확인해 그대로 쓴다 (viewModel이 필요하면 기존 방식 유지). `Label(_:systemImage:)`는 이 탭 아이템 자리에서만 기존 관례를 따른다 — 기존 MainTabView가 다른 방식을 쓰고 있으면 그 방식을 유지한다.

`Sources/Feature/Record/RecordPlaceholderView.swift`:

```swift
import SwiftUI
import Shared

/// 기록 탭 자리 — Plan 3에서 캘린더·배지·진도로 대체한다
public struct RecordPlaceholderView: View {
    public init() {}

    public var body: some View {
        ZStack {
            AppColor.Background.primary.ignoresSafeArea()
            Text(AppStrings.Tab.record)
                .font(AppTypography.title)
                .foregroundStyle(AppColor.Text.tertiary)
        }
    }
}
```

- [ ] **Step 2: 템플릿 데모 삭제 + DI·RootView·AppStrings 정리**

```bash
git rm -r Sources/Feature/Home Sources/Feature/Detail Sources/Feature/Explore
```

- `AppDIContainer.swift`: `makeHomeViewModel()`·`makeDetailViewModel(itemID:)`·`itemRepository` 프로퍼티 제거, 추가:

```swift
    @MainActor
    func makeTodayViewModel() -> TodayViewModel {
        TodayViewModel(checkInService: checkInService,
                       habitRepository: habitRepository,
                       trackingRepository: trackingRepository,
                       currency: AppCurrency.default(for: .current))
    }
```

- `RootView.swift`: `.main` 분기를 `MainTabView(appRouter: appRouter, todayViewModel: diContainer.makeTodayViewModel())`로 교체.
- `AppStrings.swift`: `Tab.home`/`Tab.explore`, `Home`, `Explore`, `Detail` enum 제거 (xcstrings의 대응 키도 제거 — `home.navigationTitle`, `explore.*`, `detail.*`, `tab.home`, `tab.explore`).
- `ExploreRouter` 참조가 남으면(AppRouter에서 제거했으므로) 컴파일 에러로 드러난다 — 잔재를 모두 지운다. `Item`/`ItemRepository`/`DefaultItemRepository`/`NetworkClient` 등 Shared 쪽 템플릿 코드는 **이 플랜에서 건드리지 않는다** (Feature에서 참조가 사라지면 그대로 두어도 무해 — 정리는 Plan 4 마무리에서).

- [ ] **Step 3: 전체 검증**

Run: `tuist generate --no-open && tuist test Shared && tuist generate --no-open && tuist build`
Expected: 테스트 전부 PASS, 앱 빌드 성공 (tuist test 후 build 전 generate 재실행 — Plan 1에서 확인된 Tuist 특이사항)

- [ ] **Step 4: 시뮬레이터 스모크 확인 (선택이 아니라 필수)**

Run:
```bash
xcrun simctl list devices available | grep -m1 "iPhone" 
xcodebuild -workspace Application.xcworkspace -scheme ServiceApp -destination 'platform=iOS Simulator,name=iPhone 17' -quiet build
```
Expected: 빌드 성공. (부팅·실행 확인은 컨트롤러가 수행)

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "feat: 탭 개편(오늘·기록·설정) + 템플릿 데모 제거 + Today DI 배선"
```

---

## Self-Review 결과

- **커버리지**: design.md 오늘 화면 지도(스트릭 헤더·소급 카드·습관 리스트·하루 마무리 CTA), closing-sheet(전원 참음 기본·잠김 행·이대로 마무리하기), lapse-sheet(메모·secondary 확정), yesterday-card(3버튼 동격·세로 폴백), freeze-chip(0개 상태), 빈 상태 CTA("첫 습관 등록하기"), 마무리 후 상태 칩 — 전부 태스크에 매핑.
- **다음 플랜으로 미룬 것**: 습관 추가 시트(온보딩과 함께 Plan 4로 가면 빈 상태 CTA가 죽은 버튼이 됨 → **Plan 3 첫 태스크로 편성할 것**), 마무리 의식 모션(1.2초 시퀀스)·토스트·배지 획득 시트·위험 배너(23시)·습관 상세, Item/Network 템플릿 잔재 정리.
- **알려진 단순화(의도)**: 소급 답변은 전 습관 일괄 적용, 즉시 기록 진입은 행 컨텍스트 메뉴(스와이프는 커스텀 행이라 List 전환 필요 — Plan 3에서 재평가).
- **타입 일관성**: `TodayViewModel.HabitRow` ↔ Task 4·5 뷰 소비 시그니처 일치. `YesterdayAnswer`·`CloseResult`는 Plan 1 공개 타입 재사용.
